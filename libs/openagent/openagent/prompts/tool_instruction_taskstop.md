Cancel a running background task by its task ID.

- Use this when a background task is no longer needed
- If the task has already completed or failed, this is a no-op
- The task's result will show as cancelled in the ${TASKOUTPUT_TOOL_NAME} output
