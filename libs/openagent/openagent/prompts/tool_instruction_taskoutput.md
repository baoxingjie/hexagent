Retrieve the result of a background task by its task ID.

- Use `block=true` (default) to wait for completion
- Use `block=false` to check status without waiting
- The `timeout` parameter controls max wait time in milliseconds
